
cd {worksapce}/SCIKIT-LEARN-Supervised-Learning

set up the virtual environment to presarve the dependecys 
python3 -m venv venv
source venv/bin/activate

install all the dependencies 
pip3 install -r requirements.txt


python3 heart_disease_demo.py

after this it will create a file with png extenshan:
heart_disease_evaluation.png
